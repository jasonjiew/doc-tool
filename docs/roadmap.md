# 下一阶段路线图（合并稿）

> 状态：盘点合并稿，未改业务代码。主链路（Word 导入 → Markdown 维护 → 搜索/替换/引用/重编号/检查 → 改动 Diff → 校验构建 → Word 刷新 → 安装发布）已完整。
> 下一阶段重点：**保真（Fidelity）· 安全（Safety）· 质量追溯（Quality Traceability）· 协作（Collaboration）**。
> 本文件是**编排层**：把 P0/P1 全部条目合并为 6 个可独立排期、独立验收的计划，每个计划对应一个 OpenSpec 变更（`openspec/changes/<name>/`）。P2 保留在文末，暂不排程。

---

## 〇、6 个计划总览与实现顺序

| 计划 | 合并能力（原条目） | OpenSpec 变更 | 状态 |
|---|---|---|---|
| **1 工作台安全与恢复** | P0-2 未保存保护/草稿 + P1-L 会话恢复（P0-1 实机验收属既有 `optimize-internal-ui-workbench` 变更收尾，非本变更范围） | `workbench-safety-recovery` | ✅ 已实施（待人工验收后归档） |
| **2 导入保真与解析统一** | P0-4 统一安全解析 + P0-3 导入保真门禁 + P1-N 样式映射向导 | `import-fidelity-and-parsing` | ✅ 已实施（待人工验收后归档） |
| **3 内容创作工作台** | P1-C 编辑器增强 + P1-D 图片与资源管理器 + 第一批建议 3 Mermaid 图形工作台 | `content-authoring-workbench` | ✅ 已实施（待全量回归阻断解除与人工验收） |
| **4 结构组织与 Word 表达** | P1-E 章节树拖拽 + P1-B Word 表达扩展 | `structure-and-word-expression` | ✅ 已实施（待全量回归与人工验收） |
| **5 质量门禁与可观测** | P1-H 问题中心 + P1-Q 机器可读 CLI + P0-5 测试/发布门禁 | `quality-gates-and-observability` | ✅ 已实施（待实机 CI/冻结包验收） |
| **6 质量追溯与交付审阅** | P1-G 质量规则 + P1-F 追踪矩阵 + P1-JK 版本基线/设置 + P1-I 版本历史 + P1-P PDF 导出 + P1-O 评审审批 + P1-M 重新导入 | `quality-traceability-and-review` | ⏳ 服务层已实施，矩阵/历史/导出/评审/基线/重导入的 UI/CLI 集成与全量回归待完成 |

**整体实现顺序（按依赖 + 价值）：**

- **阶段一：计划 1 + 计划 2**（安全/保真地基，互不依赖可并行）
  - 计划 1：未保存保护 → 自动草稿/崩溃恢复 → 会话恢复（P0-1 实机验收作为既有 `optimize-internal-ui-workbench` 变更的收尾验收单独推进）
  - 计划 2：统一 OOXML 解析 → 保真度门禁/往返差异 → 样式映射
- **阶段二：计划 3 + 计划 4**（创作体验依赖计划 1；Word 表达依赖计划 2）
  - 计划 3：编辑器增强 → 图片资源管理 → Mermaid 工作台
  - 计划 4：章节树拖拽重排 → Word 表达扩展
- **阶段三：计划 5**（门禁/可观测，作为后续的验收闸门）
  - 问题中心 → 机器可读 CLI → 测试/发布门禁
- **阶段四：计划 6**（追溯/交付/协作，依赖计划 5 输出端 + 计划 2 导入能力）
  - 质量规则 → 追踪矩阵 → 版本基线/设置页 → 版本历史对比 → PDF/HTML 导出 → 评审审批 → 重新导入

---

## 一、合并说明

原盘点 32 项中，以下条目因语义重叠做了合并（原编号保留可追溯，未丢失任何内容）：

| 合并后工作流 | 原条目 |
|---|---|
| 未保存内容保护与恢复 | 2（退出/切项目/删改文件保存保护 + 全部保存）+ 3（自动草稿与崩溃恢复） |
| 导入保真度门禁 | 4（导入前识别可能损失内容）+ 5（源 Word→Markdown→重建 Word 往返差异门禁） |
| 项目设置与版本基线 | 11（项目属性/发布设置页）+ 12（版本递增/命名检查点/冻结/恢复基线） |
| 编辑器增强 | 15 中除「全部保存」（已并入未保存保护）外的其余项 |

其余条目单列，仅按主题归组，全部纳入上表 6 个计划（P1-J/P1-K 合并为一个能力）。

---

## 二、第一批建议（优先落地）

推荐顺序即依赖序，前两项属「安全」基座，中间两项属「质量追溯/保真」，最后一项属「配置管理」：

1. **未保存内容保护与恢复**（= 计划 1 主体）→ `workbench-safety-recovery`
2. **导入保真度门禁**（= 计划 2 主体）→ `import-fidelity-and-parsing`
3. **Mermaid 图形工作台**（= 计划 3 子项）→ `content-authoring-workbench`
4. **需求—设计—测试追踪矩阵**（= 计划 6 子项）→ `quality-traceability-and-review`
5. **项目设置与版本基线**（= 计划 6 子项）→ `quality-traceability-and-review`

### 计划 3 落地映射

- **P1-C 编辑器增强** → `content-authoring-workbench`：Ctrl+H 替换、拼写检查、代码片段、Markdown 工具栏与预览双向定位。
- **P1-D 图片与资源管理器** → `content-authoring-workbench`：截图粘贴/图片拖入、自动命名与尺寸引用、未使用资源清理、缺失引用修复及可回滚删除。
- **第一批建议 3 Mermaid 图形工作台** → `content-authoring-workbench`：围栏/裸源码识别、按行校验、实时预览、PNG 导出插入 Word 与批量转换。

---

## 三、P2 中长期方向（暂不排程）

- **P2-A Git/受控协作集成**（原 25）— 提交、分支、拉取冲突提示、评审包与审计轨迹；不做实时多人编辑。
- **P2-B 跨机器并发与冲突合并**（原 26）— 共享盘场景：租约锁、内容哈希乐观锁、三方合并。
- **P2-C 大文档性能优化**（原 27）— 流式处理 ZIP/媒体、持久化增量索引、SQLite FTS、预览增量渲染、搜索分页。
- **P2-D 真实进度与细粒度取消**（原 28）— 进度下沉到章节、图片、ZIP 部件与校验项。
- **P2-E 模板与规则包插件化**（原 29）— 允许新增公司文档类型、模板、校验规则与输出策略。
- **P2-F 项目 Schema 迁移框架**（原 30）— 升级预览、自动备份、迁移、失败回滚。
- **P2-G 受控版本检查与更新**（原 31）— 签名验证、灰度更新、快速回滚。
- **P2-H 私有化 AI 写作助手**（原 32）— 辅助补全章节、审查一致性、生成图表与变更摘要；所有结果需人工确认。

---

## 四、落地机制

- 本文件是**编排层**：选中的计划各自在 `openspec/changes/<name>/` 实现（proposal → design → specs → tasks），验收通过后同步 `openspec/specs/` 并归档。
- 每个计划内部按上表「内部实现顺序」渐进，apply/验收以一个计划为单位。
- 每完成一个计划，更新本文件总览表的状态列，标注「已归档」。
