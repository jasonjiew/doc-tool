#### 数据处理

<EMPTY_PAR/>
<!-- TBL:style=44 type=auto tw=0 cols=1764,8264 cm=0,0,0,0 ind=0:dxa lay=autofit bd=single:auto:4:0 hdr=0 tcm=90,195,90,195 va=center -->
| 功能 | 核心逻辑描述 |
| --- | --- |
| 数据清洗 | 使用 StringUtils.replaceEach 方法移除接收到的数据中的换行符和空格，以确保数据的纯净和一致性。 |
| 数据记录 | 调用 log.info 记录处理后的数据，便于后续审查和问题追踪。 |
| 数据分割与解析 | 使用 String.split 方法，按逗号分割数据字符串，将数据分解为数组 recvArr。数据的第一个元素 recvArr[0] 作为命令标识符，用于确定后续处理逻辑。 |
| 命令处理逻辑 | 根据 recvArr[0] 的值，执行不同的操作： COMMAND_P：调用 handleCommandP(recvArr, data) 方法处理相关数据。 提取设备码：从recvArr中提取设备码deviceCode。 生成缓存键：根据设备码生成settingKey、triggerKey和onlineKey。 时间范围判断：判断接收时间是否在11:58:30到11:59:00之间，如果是，则将数据发送到消息队列。 设置设置键：将数据存储到settingKey，设置过期时间为10分钟。 检查触发键：如果triggerKey不存在，则生成一个随机过期时间并设置triggerKey。 在线统计：设置onlineKey，过期时间为10秒。 COMMAND_D：调用 handleCommandD(recvArr, data) 方法处理相关数据。 提取设备码和数据库名：从 recvArr 中提取设备码 deviceCode 和数据库名 dbName。 转换时间戳：将 recvArr 中的时间戳转换为日期格式 cureTime。 构建 Redis 键名：使用 buildRedisKey 方法构建两个 Redis 键名 redisListKey 和 triggerRedisKey。 存储数据到 Redis 列表：将 data 存储到 redisListKey 对应的 Redis 列表中。 检查并设置触发键：如果 triggerRedisKey 不存在，则生成一个随机的过期时间，并设置该键。 COMMAND_S 和 COMMAND_T： 异步方式处理，使用 CompletableFuture.runAsync 提交到线程池 breatheNettyTaskExecutor。 数据被发送到消息队列（通过 streamBridge.send），适用于需要延迟处理的数据。 COMMAND_E： 与 S/T 类似，但可能涉及特定的业务逻辑，也是异步发送到另一个消息队列。 默认情况：如果接收到的数据不符合预期的命令格式，则记录非法参数并关闭连接。 |
| 过期监听事件（D指令） | processMinListKey 方法用于处理 Redis 中过期的触发密钥。具体步骤如下： 移除触发密钥前缀：从 expiredKey 中移除 "TRIGGER:" 前缀，获取实际的数据键 dataKey。 获取数据列表：从 Redis 列表中获取 dataKey 对应的所有数据。 检查数据列表是否非空： 如果数据列表非空，将数据列表转换为 JSON 字符串，并提取数据键中的数据库索引部分。 根据数据库索引格式化消息队列名称，发送数据到消息队列。 删除 Redis 中的数据键。 如果数据列表为空，记录错误日志。 |
| 过期监听事件（P指令） | processSettingKey 方法的主要功能是处理过期的 Redis 键，具体步骤如下： 提取数据键：从 expiredKey 中移除前缀 "TRIGGER:"，得到实际的数据键 dataKey。 获取数据值：从 Redis 中获取 dataKey 对应的值 valueData。 检查数据值： 如果 valueData 不为空，则将其发送到消息队列，并删除 dataKey。 如果 valueData 为空，则记录错误日志。 |
| 解析处理（P指令） | 定义了一个消息消费者 mqBreatheDeviceSettingDataSmg，用于消费通过WIFI上传的设备设置信息，并将其保存到Redis中。具体步骤如下： 启动计时器：使用 StopWatch 记录整个处理过程的时间。 获取最新记录：调用 getLatestRecords 方法，从消息列表中提取每个设备的最新记录。getLatestRecords 处理逻辑 初始化：创建一个 ConcurrentHashMap 来存储每个设备的最新记录。 并行处理：使用并行流处理每个记录，提高处理速度。 分割记录：将每条记录按逗号分割，提取设备号和时间戳。 更新记录：使用 compute 方法更新 ConcurrentHashMap 中的记录，如果当前记的时间戳大于已存储的记录时间戳，则替换为当前记录。 返回结果：将 ConcurrentHashMap 中的所有最新记录转换为列表并返回。 处理每条记录： 将记录转换为 BreatheDeviceSettingRecordDto 对象。 从缓存中获取患者ID并设置到对象中。 将处理后的对象添加到列表中。 调用 isDataUploadedTodayCache 方法，将设备上传数据的信息缓存到Redis中。 注册设备设置：调用 remoteBreatheDeviceService.breatheWifiRegisterBySetList 方法，将处理后的设备设置信息注册。 记录日志：记录整个处理过程的总耗时。 异常处理：捕获并记录任何可能发生的异常。 |
| 解析处理（E指令） | 定义了一个Spring Bean mqBreatheDeviceEventDataSmg，用于消费消息队列中的设备事件数据。主要功能包括： 初始化计时器：使用 StopWatch 记录总处理时间。 转换消息：将接收到的消息列表转换为 BreatheEventInfoBo 对象列表。 按设备分组：将 BreatheEventInfoBo 对象按设备序列号分组。 异步处理数据：对每个设备序列号，异步处理其对应的事件数据，将其转换为 SysCuredataEvent 对象并批量插入数据库。 记录日志：记录总处理时间。 |
| 解析处理（D指令） | 定义了一个Spring Bean mqBreatheDeviceCureDataSmg，用于消费消息队列中的设备每秒治疗数据。主要功能包括： 数据分组：使用并行流对消息进行分组，按设备编号和时间戳进行分组。 获取患者ID：从缓存中获取每个设备对应的患者ID。 异步处理数据：对每个设备的数据进行异步处理，处理过程中： 将每秒数据转换为分钟数据。 检查数据库中是否存在相同时间的分钟数据，如果存在则更新，否则新增。 批量保存数据：将处理后的分钟数据批量保存到数据库中。 |
| 解析处理（S/T指令） | 这段代码定义了一个RabbitMQ消息消费者 mqBreatheDeviceDataSmg，用于处理接收到的消息。主要功能包括： 获取消息通道和消息编号：从消息头中获取当前的RabbitMQ通道和消息编号。 解析消息内容：将消息内容按逗号分割成数组，并根据消息头的不同命令进行不同的业务处理。 业务处理： COMMAND_S：处理设备使用开始记录，保存到数据库。 COMMAND_T：处理设备使用结束记录，保存到数据库。 WIFI的标记数据类型为  0:临时  1:已正确处理  2:手动干预 WIFI传的开始和结束时间状态为 0：临时 1.只传递开始时间 1) 查询是否有未结束的开始时间，如果有则作废删除 2) 查询是否有相同时间的开始时间，如果没有则新增 例如： S                     T 2024-10-16 14:00:21                           0  删除 2024-10-16 16:00:30                           0  保留 2.只传递结束时间， 1)查询最后一条结束时间为空的数据， 2)判断开始时间和结束时间是否跨天，如果跨天则按照1159划分 3)设置标记为 2 程序手动设置跨天时间点 S                      T 2024-10-16 08:00:30   2024-10-16 11:59:00     2 修正后：2024-10-16 08:00:30 ~ 2024-10-16 10:59:00     0 2024-10-16 11:59:00   2024-10-17 11:59:00     2 2024-10-17 11:59:00   2024-10-18 14:33:00     2 处理逻辑（触发点：查询报告）： 1)查询标记为2的记录，查询秒级库，按照最后的时间缩短当前时间，生成的新记录，标记为0 2)导入SD卡，把当前设备当前人状态为0的删除全部删除，以SD卡为主 |

<EMPTY_PAR/>
#### 协议兼容

<!-- TBL:style=44 type=auto tw=0 cols=1764,8264 cm=0,0,0,0 ind=0:dxa lay=autofit bd=single:auto:4:0 hdr=0 tcm=90,195,90,195 va=center -->
| 功能 | 核心逻辑描述 |
| --- | --- |
| P指令兼容 | 将呼吸机的设置数据转换为实体类 BreatheDeviceSettingRecordDto。具体步骤如下： 输入校验：检查输入字符串 data 是否为空，如果为空则抛出 IllegalArgumentException。 数据分割：将输入字符串 data 按逗号分割成数组 recvArr。 协议版本处理：根据 recvArr 的长度判断协议版本，并调用相应的处理方法： 协议 1.0.0：调用 handleSecSetProtocol100（长度24） 协议 1.0.1：调用 handleSecSetProtocol110（长度26） 协议 1.0.2：调用 handleSecSetProtocol120（长度30） 特殊处理：对不同模式的参数设置进行特殊处理，调用 BreatheParamSettingHandler 的 matchModeEliminate 方法。 返回结果：返回处理后的 deviceSettingRecordDto。 |
