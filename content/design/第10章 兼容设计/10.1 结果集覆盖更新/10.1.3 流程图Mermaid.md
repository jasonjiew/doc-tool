<!-- P:line=240;lr=auto -->
graph TD

<!-- P:line=240;lr=auto -->
Start(开始) --> CheckResult[结果集是否存在]

<!-- P:line=240;lr=auto -->
CheckResult -- 是 --> TimeComparison[治疗结束时间比对处理]

<!-- P:line=240;lr=auto;first=420 -->
CheckResult -- 否 --> AddNewResult[新增结果集]

<!-- P:line=240;lr=auto;first=420 -->
AddNewResult--> SaveOrUpdateDevice[保存或更新设备信息、最后使用记录、设置信息]

<!-- P:line=240;lr=auto -->
TimeComparison --> CompareTime[治疗时间大于库里记录，或治疗时间等于且优先级大于库里记录]

<!-- P:line=240;lr=auto -->
CompareTime -- 是 --> UpdateResult[更新结果集]

<!-- P:line=240;lr=auto -->
CompareTime -- 否 --> SaveUsageRecord[保存、更新使用记录]

<!-- P:line=240;lr=auto -->
UpdateResult --> SaveOrUpdateDevice[保存或更新设备信息、最后使用记录、设置信息]

<!-- P:line=240;lr=auto -->
SaveOrUpdateDevice --> SaveUsageRecord[保存、更新使用记录]

<!-- P:line=240;lr=auto -->
SaveUsageRecord --> SetCache[设置缓存，5秒后触发使用记录合并修正]

<!-- P:line=240;lr=auto -->
SetCache --> End(结束)
