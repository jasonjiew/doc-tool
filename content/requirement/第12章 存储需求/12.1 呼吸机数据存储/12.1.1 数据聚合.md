<EMPTY_PAR/>
<!-- TBL:style=143 type=dxa tw=10281 cols=2343,3402,2693,1843 cm=0,108,0,108 ind=-108:dxa lay=autofit bd=single:auto:4:0 hdr=0 va=bottom -->
| 需求标识 | 要求说明 | 验收标准 | 备注 |
| --- | --- | --- | --- |
| REQ- DATA-STORE-001 | 聚合适用范围：仅针对呼吸机秒级实时数据（用于每日详情波形展示的字段），按设备ID、患者ID、日期维度聚合为分钟级数据 | 1. 聚合数据仅包含波形展示所需字段，无冗余信息；<br>2. 聚合维度准确，可通过设备ID+患者ID+日期+分钟时间戳唯一定位一条聚合数据 | 需业务侧确认波形展示所需字段清单，确保无遗漏 |
| REQ- DATA-STORE-002 | 字段聚合规则：<br>1. 漏气量：无大量漏气事件时取分钟内有效秒级数据平均值，有大量漏气事件时取分钟内最大值；<br>2. 呼吸频率、潮气量、流量、吸呼比：取分钟内有效秒级数据算术平均值；<br>3. 吸气压力：基于滑动窗口算法计算；（单水平的治疗压力取吸气压）<br>4. 呼气压力：基于滑动窗口算法计算 | 1. 各字段聚合结果符合上述规则，计算逻辑无错误；<br>2. 呼吸模式识别准确，不同模式下的吸/呼气压力计算结果与设备实际工作状态一致；<br>3. 聚合后数据与原秒级数据的波形趋势一致性误差≤5%；<br>4. 有效数据判定规则：剔除异常值 | 1. 滑动窗口算法窗口大小需结合设备采样频率设定；<br>2. 大量漏气事件判定标准：分钟内漏气量超阈值（阈值由业务侧提供）的秒级数据占比≥30%；<br>3. 设备工作参数范围需从设备配置表读取，支持动态适配 |
| REQ- DATA-STORE-003 | 分钟级数据存储：新增分钟级数据专用数据表，表结构需兼容原波形展示查询逻辑，支持按设备ID、患者ID、时间范围（日期+分钟）快速查询 | 1. 数据表设计包含索引（设备ID+时间戳联合索引），单设备单天数据查询响应时间≤100ms；<br>2. 数据存储格式与原秒级表字段类型一致，包含字段：设备ID、日期、分钟时间戳（精确到分钟）、各波形字段聚合值、呼吸模式标识；<br>3. 支持数据增量写入，不重复存储同一设备同一分钟的聚合数据 | 表结构需与原秒级表保持字段命名一致性，便于查询逻辑兼容 |
| REQ-VENT-004 | 实时展示切换：波形展示功能默认读取分钟级数据 | 1. 波形展示界面加载延迟≤300ms，无明显卡顿；<br>2. 分钟级数据波形与原秒级数据波形趋势一致，无明显失真；<br>3. 用户无感知数据来源切换，操作体验与优化前一致 |  |

<EMPTY_PAR/>
