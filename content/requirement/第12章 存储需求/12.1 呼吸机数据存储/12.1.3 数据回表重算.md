<EMPTY_PAR/>
<!-- TBL:style=143 type=dxa tw=10281 cols=1632,3177,3138,2334 cm=0,108,0,108 ind=-108:dxa lay=autofit bd=single:auto:4:0 hdr=0 va=bottom -->
| 需求标识 | 要求说明 | 验收标准 | 备注 |
| --- | --- | --- | --- |
| REQ- DATA-STORE -007 | 触发条件：当机器当日有历史数据上传时，触发次日定时回表重算任务；任务需自动识别上传的历史数据对应的设备ID、日期范围，从OSS归档文件中提取对应秒级数据回表 | 1. 系统可准确捕获机器历史数据上传事件，记录设备ID、数据日期范围，无遗漏或误触发；<br>2. 回表数据自动匹配设备ID和日期范围，无需人工干预；<br>3. 回表数据完整，无字段丢失，回表时间≤10分钟/10天数据 | 1. 历史数据上传事件需包含设备ID、数据起始时间、数据结束时间等关键信息；<br>2. 回表数据存储于临时表，校验无误后再参与重算 |
| REQ- DATA-STORE -008 | 定时任务配置：每日固定时间执行重算任务，任务仅处理当日有历史数据上传的设备对应的相关日期范围数据 | 1. 定时任务可正常启动、执行，支持手动触发和配置执行时间；<br>2. 任务仅处理符合条件的设备数据，不重复处理无历史数据上传的设备；<br>3. 任务执行过程不占用核心业务资源，不影响实时数据采集与展示 | 1. 任务执行日志需详细记录处理的设备ID、日期范围、执行时长、结果； |
| REQ- DATA-STORE -009 | 聚合重算规则：重算时严格遵循REQ- DATA-STORE -002规定的字段聚合规则和呼吸模式适配规则，重算结果覆盖原有分钟级数据 | 1. 重算结果准确，与最新聚合规则一致，无数据重复或缺失；<br>2. 重算后的数据与原秒级数据（含新增历史数据）的波形趋势一致性误差≤3%；<br>3. 重算完成后，波形展示数据同步更新为最新重算结果 | 1. 重算前需备份原有分钟级数据，支持回滚；<br>2. 若同一设备同一日期多次上传历史数据，仅需保留最新一次重算结果 |
