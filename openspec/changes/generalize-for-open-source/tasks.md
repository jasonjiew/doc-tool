## 1. 发布决策与现状基线

- [x] 1.1 记录当前内部版应用版本、项目 schema、安装器 AppId、可执行文件、安装目录和设置命名空间，形成可回滚基线
- [x] 1.2 将公共显示名、程序标识、组织设置键、图标、域名、著作权主体和许可证列为发布决策项，未知项保持显式阻断而不填入猜测值
- [x] 1.3 运行并保存当前全量测试、冻结应用冒烟和至少一个旧 `requirement` / `design` 项目的构建基线结果
- [x] 1.4 建立禁止品牌、内部编号、内部域名、RFC1918 地址和敏感路径的初始扫描词表，并由权利人/安全审核人确认

## 2. 集中品牌与应用身份

- [x] 2.1 新增无 Qt 依赖的品牌/发行元数据模块，集中定义显示名、程序标识、组织设置键、CLI 名、可执行文件名和公共 URL
- [x] 2.2 将 `doc_tool/app.py`、主窗口、空状态、关于页、CLI 描述、序列化输出、日志和错误提示改为读取集中元数据
- [x] 2.3 将 PyInstaller onedir/onefile 的应用名和可执行文件改为公共标识，并更新冻结态资源定位与允许清单
- [x] 2.4 将 Inno Setup 的 AppName、Publisher、URL、AppId、安装目录、快捷方式和输出文件名改为公共身份，确保与内部版可并存安装
- [x] 2.5 更新 GitLab CI、`packaging/build.ps1`、SBOM 生成、泄漏扫描、allowlist 和发布说明中的产物名称与路径
- [x] 2.6 添加品牌一致性测试，扫描生产代码、打包脚本和候选产物，阻断公司名称、网址、内部编号或旧可执行文件名

## 3. 设置和安装迁移

- [x] 3.1 定义允许从旧 QSettings 命名空间迁移的字段白名单，包括窗口几何、主题、Dock/标签状态和最近项目
- [x] 3.2 实现首次启动只读迁移：仅在公共设置为空时复制允许字段，不删除旧值且不覆盖后续公共设置
- [x] 3.3 添加设置迁移的首次执行、幂等、字段过滤、损坏旧设置和无旧设置测试
- [x] 3.4 添加公共版与内部版并存安装、独立升级、独立卸载和可执行文件存活检查

## 4. 通用项目新建模型

- [x] 4.1 在项目领域模型中区分可新建类型、旧兼容类型和可读取类型，同时保持 schema v1 读取 `general` / `requirement` / `design`
- [x] 4.2 修改导入向导，移除需求/详细设计单选项和模式建议，只展示通用大文档流程，并将编号与版本设为可选
- [x] 4.3 修改导入预检，移除基于“需求/详细设计”关键词的公共类型建议，保留标题映射、保真和往返风险报告
- [x] 4.4 修改公共 CLI，使 `import` 只创建 `general` 项目并从帮助中移除旧类型；对显式旧类型参数返回清晰错误且不创建目录
- [x] 4.5 调整清单校验与输出命名，使空编号/空版本的通用项目生成安全、稳定的文件名
- [x] 4.6 更新默认项目资源、项目设置、项目栏和质量默认值，确保普通手册不因缺少公司编号、需求或设计章节而报错
- [x] 4.7 添加 GUI/CLI 通用导入、可选元数据、业务关键词不改变类型、非标准标题映射和失败无半成品测试

## 5. 旧项目兼容与迁移

- [x] 5.1 保留并隔离 `requirement` / `design` 清单、路径、资源和专用质量规则的兼容加载路径，新项目代码不得再产生这些类型
- [x] 5.2 在项目栏/关于项目信息中标记“旧版专用项目”，并说明兼容状态，不静默修改 `project.yml`
- [x] 5.3 为旧项目迁移定义复制目标、备份、路径重写、资源引用重写、状态处理和迁移报告格式
- [x] 5.4 实现旧项目迁移到新目录的通用项目流程，在结构、资源和试构建验证全部通过后才提交结果
- [x] 5.5 添加迁移失败回滚、源项目不变、重复迁移、缺失资源、损坏模板和 Word 不可用诊断测试
- [x] 5.6 使用完全人工合成的最小旧 `requirement` / `design` 夹具运行读取、编辑、校验和兼容构建回归

## 6. 内容工作台业务中性化

- [x] 6.1 修改内容索引和章节树，使通用单项目直接展示章节节点，旧多类型布局才展示兼容类型根
- [x] 6.2 修改搜索面板和服务，使通用项目默认搜索全部内容且不展示需求/详细设计筛选，旧多类型项目保留兼容过滤
- [x] 6.3 同步调整替换、问题中心、图片资源、引用分析和树标签中的业务类型文案与筛选显示逻辑
- [x] 6.4 将默认质量规则改为业务中性规则；将需求/设计专用规则限制在旧项目兼容适配或私有扩展接口
- [x] 6.5 评估追踪矩阵入口，将固定“需求→设计→接口→验收”从公共默认界面隐藏或改为可选中性能力，同时保留数据兼容
- [x] 6.6 更新树、搜索、替换、问题、资源和质量规则测试，使用通用虚构路径并保留最小旧布局兼容场景

## 7. 启动入口与旧专用构建退出公共产品面

- [x] 7.1 新增中性名称且纯 ASCII 内容的公共启动脚本，保留 PySide6 探测、vendor 路径和引导安装语义
- [x] 7.2 从公共发行清单移除旧品牌启动脚本以及需求、详细设计和全部生成 `.cmd` 兼容入口
- [x] 7.3 将仓库内置 `build requirement|design|all` 入口标记为内部兼容或从公共 CLI 删除，确保 `build --project` 仍支持通用和旧项目
- [x] 7.4 更新启动、CLI 帮助、ASCII 字节、错误码和旧入口缺席的自动测试

## 8. 公共资源边界与脱敏示例

- [ ] 8.1 从公共源码导出清单中排除公司专用 `templates/`、`config/`、`content/`、`assets/`、`analysis/` 和 `migration/legacy/`
- [ ] 8.2 修改 PyInstaller spec，不再隐式遍历根模板和配置；把运行必需资源移入 `doc_tool/resources/` 并逐项登记
- [ ] 8.3 从零创建虚构的大文档 DOCX、Markdown、图片和复杂表格示例，不复用内部章节、截图、型号、文档编号或布局
- [ ] 8.4 用虚构夹具替换生产代码与测试中的 `KF-2090`、康尚、健康云、KSHC/KSOA 等内部标识和业务文本
- [ ] 8.5 对 DOCX/OOXML ZIP 内部 XML、关系、文档属性、图片元数据和文件名执行内容扫描，并添加正负测试
- [ ] 8.6 建立精确路径公共资源 allowlist；任何未登记顶层资源、DOCX、图片或复杂表格进入 `dist` 时阻断构建
- [ ] 8.7 在独立临时目录生成净化公开源码清单并验证不包含被排除目录或隐藏/未跟踪内部文件

## 9. 公共发行安全与供应链门禁

- [ ] 9.1 扩展泄漏扫描，覆盖 Git 跟踪文件、打包输入、`dist`、安装器与 Release 目录中的品牌、内网、凭据和文档资源
- [ ] 9.2 将依赖审计、CycloneDX、SPDX、第三方许可证通知和主许可证一致性校验接入公共 CI
- [ ] 9.3 添加公共正式发布授权门禁：未记录著作权、品牌、许可证、历史清理和安全批准时只允许内部测试产物
- [ ] 9.4 在干净 Windows Runner 执行版本一致性、全量测试、敏感扫描、冻结构建、冻结冒烟、安装/卸载和 Word 实机验收
- [ ] 9.5 生成包含版本、提交、校验和、SBOM、已知限制和升级说明的可审计公共 Release 说明

## 10. 开源治理文件

- [ ] 10.1 在权利人确认后添加标准 `LICENSE` 全文，并统一 README、包元数据和 SBOM 中的 SPDX 标识
- [ ] 10.2 添加 `CONTRIBUTING.md`，说明开发环境、测试、提交规范、版权贡献约定和禁止敏感数据规则
- [ ] 10.3 添加 `CODE_OF_CONDUCT.md` 及执行/联系信息
- [ ] 10.4 添加 `SECURITY.md`，说明支持版本、私密报告渠道、响应流程和恶意文档样本处理要求
- [ ] 10.5 添加 GitHub/GitLab Issue 模板、Pull Request 模板和必要的贡献者检查项
- [ ] 10.6 更新 `THIRD_PARTY_LICENSES.txt`，确保安装包携带适用通知并与锁定依赖一致

## 11. 文档、品牌资产与发布验收

- [ ] 11.1 更新 README 为最终公共名称、真实仓库地址、许可证、安装方式、脱敏截图、限制和安全入口，移除“开源准备中”占位状态
- [ ] 11.2 准备经授权的中性图标、应用元数据和四类脱敏截图，并验证图片不含人员、客户、路径或内部系统信息
- [ ] 11.3 编写旧内部版到公共版的安装、设置和项目迁移指南，明确并存、回滚和不兼容入口
- [ ] 11.4 运行全量自动测试、OpenSpec 验证、依赖审计、泄漏扫描、冻结与安装级冒烟测试并归档报告
- [ ] 11.5 在真实 Word 上执行导入、编辑、诊断构建、正式刷新、刷新后校验和视觉检查，确认上一份有效输出不会被失败任务覆盖
- [ ] 11.6 由著作权/品牌、安全、依赖许可和发布负责人逐项签署公开检查单后，发布预发行版本并收集反馈
