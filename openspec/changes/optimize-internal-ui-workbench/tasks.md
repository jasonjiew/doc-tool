## 1. 展示状态与样式基础

- [x] 1.1 梳理 `MainWindow` 现有项目、任务、Word 就绪、输出与报告状态来源，定义可独立测试的工作台展示状态和动作可用性映射
- [x] 1.2 将菜单项与首屏快捷按钮统一到共享命令和统一状态刷新入口，保持现有确认、错误处理和快捷键行为
- [x] 1.3 在 `doc_tool/ui/styles.py` 集中定义字体层级、标准间距、主要/次要按钮及中性/成功/警告/失败语义样式
- [x] 1.4 为高 DPI 和系统主题补充样式降级规则，确保状态含义同时由文字表达而非仅依赖颜色

## 2. 未打开项目空状态

- [x] 2.1 在主内容区实现未打开项目空状态容器，提供“新建项目”和“打开项目”直接入口
- [x] 2.2 复用现有最近项目存储与打开逻辑，在空状态展示有效最近项目并处理路径失效反馈
- [x] 2.3 实现空状态与已打开项目工作台的切换，确保菜单、状态栏、窗口几何和关闭行为不受影响

## 3. 已打开项目工作台

- [x] 3.1 重组项目摘要为可执行状态摘要，显示项目名称、路径、文档类型、兼容/可写状态和正式合并就绪状态
- [x] 3.2 增加正式合并、诊断构建和项目校验首屏入口，并委托给现有命令处理逻辑
- [x] 3.3 增加打开内容目录、输出位置和报告的首屏入口，并在路径不存在时禁用入口和显示原因
- [x] 3.4 实现项目只读、Word 不可用、任务冲突和缺少产物等不可用原因展示，保证菜单与按钮状态一致

## 4. 任务阶段与取消反馈

- [x] 4.1 将现有 `stage/progress/warning/error/result` 事件映射到任务名称、当前阶段、确定性进度或心跳及已用时间展示
- [x] 4.2 保持现有安全取消边界，在取消请求后显示等待安全阶段的状态，并在受保护临界区正确禁用取消入口
- [x] 4.3 统一任务启动、成功、失败、取消和超时后的工作台刷新，避免旧任务状态或冲突入口残留

## 5. 持久任务结果与分层错误

- [x] 5.1 实现主窗口结果区域及空闲、运行中、成功、失败和取消状态，结果保持到下一任务、项目切换或明确清除
- [x] 5.2 在正式合并成功时提供打开产物、打开所在目录和查看校验报告操作，并在独立校验成功时只显示适用操作
- [x] 5.3 在渲染和执行结果操作前验证路径存在性，项目切换后清除不属于当前项目的结果
- [x] 5.4 对已知失败显示用户原因和建议操作，并提供可展开的错误码、失败阶段、异常摘要和日志路径
- [x] 5.5 对未知异常和发布前失败提供脱敏通用反馈，明确区分上次有效输出与本次未生成的新产物

## 6. 可折叠事件日志

- [x] 6.1 将事件日志调整为默认紧凑的摘要栏，并实现展开和折叠交互
- [x] 6.2 保持折叠期间事件接收与待读计数，展开后沿用现有查看和待读清除语义
- [x] 6.3 在展开日志中保留复制日志内容和打开日志目录能力，验证折叠操作不影响后台任务和结果更新

## 7. 自动化测试

- [x] 7.1 扩展 `scripts/tests/test_gui_services.py`，覆盖无项目、有效/失效最近项目和空状态到工作台切换
- [x] 7.2 增加项目可写、只读、Word 不可用、任务运行和缺少产物的动作状态矩阵测试
- [x] 7.3 增加菜单与快捷按钮共享命令、同步启停和任务生命周期刷新测试
- [x] 7.4 增加确定性进度、心跳、安全取消等待和终态渲染测试
- [x] 7.5 增加成功结果适用操作、失效路径、项目切换及失败原因/建议/技术详情测试
- [x] 7.6 增加日志折叠、折叠期间待读计数、展开查看和任务事件不中断测试
- [x] 7.7 运行完整自动化测试，确认项目服务、任务桥接、文档构建和校验行为无回归

## 8. 人工验收与打包验证

- [ ] 8.1 使用 `python -m doc_tool.app` 验证无项目启动、新建/打开/最近项目、菜单和键盘快捷键兼容性
- [ ] 8.2 人工验证可写项目、只读项目、Word 不可用、输出缺失以及中文长路径下的工作台状态和首屏布局
- [ ] 8.3 人工执行校验、诊断构建、正式合并、失败、取消和超时路径，确认阶段、结果后续操作、分层错误和日志折叠行为
- [ ] 8.4 在支持的 Windows DPI/主题设置下检查标题、正文、按钮、状态文字和窗口最小尺寸的可读性
- [ ] 8.5 运行现有 PyInstaller onedir 构建与冻结程序启动冒烟测试，确认资源、图标和 UI 样式在冻结态正常
- [ ] 8.6 在实现验收通过后运行完整安装器构建流水线，验证安装版启动和核心工作台操作且不改变用户项目数据
