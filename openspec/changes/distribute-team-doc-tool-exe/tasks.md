## 1. 工程基线与项目模型

- [x] 1.1 建立 `doc_tool` 分层包结构和 GUI/CLI 入口，并让现有测试在重构前继续通过
- [x] 1.2 定义 `project.yml` v1 模式、字段校验和示例项目清单
- [x] 1.3 实现 `ProjectManifest` 的加载、保存、版本兼容和清单备份
- [x] 1.4 实现 `ProjectPaths` 相对路径解析、项目根包含校验和中文特殊路径测试
- [x] 1.5 实现统一应用版本、提交标识、项目模式版本及构建信息模块
- [x] 1.6 增加项目工作区夹具，并验证完整项目可复制到另一绝对路径后打开

## 2. 现有文档内核项目化

- [x] 2.1 将构建器的模板、内容、资源、配置和输出路径改为由项目上下文传入
- [x] 2.2 将严格校验器改为由项目上下文传入路径，同时保留 `requirement/design` 旧入口
- [x] 2.3 将 Word 刷新器改为接收项目输出和超时，不再依赖仓库固定目录
- [x] 2.4 将日常管线重构为可返回结构化阶段事件和稳定错误码的应用服务
- [x] 2.5 为现有两个文档生成项目清单，并验证新旧入口的事件流、模板语义和最终正文一致
- [x] 2.6 增加 Unicode、空格、括号、`&` 及路径越界的项目化回归测试

## 3. Word 导入预检

- [x] 3.1 实现任意文件名 DOCX 的扩展名、ZIP、CRC、XML 和加密/损坏检测
- [x] 3.2 实现 relationship 目标完整性及正文资源引用预检
- [x] 3.3 实现 styles.xml 的 styleId 到 Heading 1~6 映射和标题树解析
- [x] 3.4 实现无 Heading 1、标题层级跳跃和无法闭合树的 fail-closed 校验
- [x] 3.5 实现需求/详细设计类型显式选择、元数据建议和低置信度不自动决定规则
- [x] 3.6 生成导入预览模型，包含标题级别计数、首尾标题、图片/表格数量和告警
- [x] 3.7 为改名 DOCX、无标题样式、损坏关系和中文长路径增加预检正负向测试

## 4. 事务化首次拆分

- [x] 4.1 把 `make_template.py` 重构为接收源 DOCX 与目标路径的无全局副作用函数
- [x] 4.2 把 `extract_docx.py` 重构为接收源 DOCX 与暂存工作区的无固定文件名导入器
- [x] 4.3 把 `split_content.py` 重构为接收指定输入/输出目录且不删除正式项目内容的拆分器
- [x] 4.4 实现源文档只读复制、SHA-256、项目清单和模板/内容/资源目录创建
- [x] 4.5 实现暂存目录导入、结构校验、试构建及同卷原子重命名发布
- [x] 4.6 实现目标项目已存在时预写入拒绝，禁止隐式覆盖或合并 Markdown
- [x] 4.7 实现失败/取消后的暂存隔离与诊断日志，不修改源文档或正式项目
- [x] 4.8 增加父正文 `_index.md`、普通/复杂表格、图片和事件顺序的导入回归
- [x] 4.9 使用两份现有基线和至少一份改名公司 DOCX 完成端到端首次导入验收

## 5. 项目锁、日志与错误恢复

- [x] 5.1 实现带主机、PID、启动时间和任务类型的本地项目锁
- [x] 5.2 实现活动锁拒绝、陈旧锁识别及受控清理流程
- [x] 5.3 定义结构化错误类型、稳定错误码、用户说明和建议操作
- [x] 5.4 实现脱敏轮转日志，记录阶段、版本、哈希、计数和异常但不记录正文内容
- [x] 5.5 实现阶段边界取消令牌，并保护 Word 保存与原子发布临界区
- [x] 5.6 增加并发启动、陈旧锁、取消和失败不覆盖上次输出的破坏性测试

## 6. Windows 桌面界面

- [x] 6.1 使用 tkinter/ttk 建立主窗口、项目摘要、状态栏和高 DPI 基础适配
- [x] 6.2 实现新建项目向导：选择源 Word、文档类型、项目名称和目标父目录
- [x] 6.3 实现导入预览与确认页面，展示标题/资源计数和阻断告警
- [x] 6.4 实现打开已有项目、最近项目列表和不兼容项目只读状态
- [x] 6.5 实现打开 Markdown、输出和日志目录的系统文件管理器操作
- [x] 6.6 实现独立项目校验及通过/失败计数、错误定位和重试操作
- [x] 6.7 实现正式合并与明确标记的无 Word 诊断构建入口
- [x] 6.8 实现后台任务事件桥接、阶段进度/心跳、安全取消和完成通知
- [x] 6.9 实现“关于/环境诊断”，显示版本、提交、模式、Windows 和 Word 可用性
- [x] 6.10 增加 GUI 服务层自动测试和关键界面人工操作清单

## 7. Word 正式发布闭环

- [x] 7.1 在 GUI 合并前增加 Microsoft Word 可用性和交互式会话检查
- [x] 7.2 复用专用 DispatchEx 进程实现 TOC、页码和 NUMPAGES 刷新，不影响用户 Word
- [x] 7.3 将预检、临时构建、前校验、Word 刷新、后校验和原子发布接入统一用例
- [x] 7.4 实现正式/诊断输出状态元数据，禁止未刷新结果显示为正式成功
- [x] 7.5 增加 Word 缺失、超时、保存失败和后校验失败时保留旧输出的测试
- [x] 7.6 在现有 349 页需求文档和 576 页详细设计文档上执行最终实机回归与视觉抽检

## 8. EXE 与安装器打包

- [x] 8.1 建立锁定的运行依赖和构建依赖清单，记录第三方许可
- [x] 8.2 实现统一 `resource_root()`，验证开发态与冻结态资源定位一致
- [x] 8.3 编写 PyInstaller spec，显式收集 lxml、Pillow、pywin32、模板、默认配置和图标
- [x] 8.4 构建 onedir 应用并在无系统 Python 的干净临时环境运行启动/拆分冒烟
- [x] 8.5 编写具有稳定 AppId、按用户安装、开始菜单和可选桌面快捷方式的安装器配置
- [x] 8.6 验证静默安装、覆盖升级、修复和卸载只管理应用文件
- [x] 8.7 验证升级/卸载不删除外部项目，旧应用拒写更高模式项目
- [x] 8.8 生成版本化 `KonsungDocTool-Setup-X.Y.Z.exe`、SHA-256 和依赖清单

## 9. 私有 Git 与 Release 自动化

- [ ] 9.1 建立 `.gitignore` 和打包允许清单，排除真实项目、生产输出、日志、密钥及未净化 DOCX
- [ ] 9.2 编写平台无关的 `packaging/build.ps1`，本地和 CI 使用同一构建逻辑
- [ ] 9.3 建立 Windows CI，校验标签、应用和安装器版本一致且工作树来源可追溯
- [ ] 9.4 在 CI 串联单元、契约、负向、导入/迭代、冻结程序和安装级测试
- [ ] 9.5 增加生产文档泄漏扫描，发现未允许 DOCX 或 `projects/output/logs` 时阻断打包
- [ ] 9.6 接入公司代码签名证书；未配置证书时明确产出仅限受控试点的状态
- [ ] 9.7 自动生成发布说明、哈希并把安装器上传到私有 Git Release
- [ ] 9.8 对一次测试标签执行完整 Release 演练并记录安装、哈希和回滚证据

## 10. 迁移、试点与交付

- [ ] 10.1 编写团队安装、新建项目、首次拆分、维护、校验、合并和故障处理手册
- [ ] 10.2 编写旧仓库目录迁移为自包含项目的工具，并在写入前备份清单与内容
- [ ] 10.3 选择至少两名非开发成员在无 Python 电脑完成安装和核心操作试点
- [ ] 10.4 验证需求新增/修改/删除 Markdown 后一键合并及模板体系保留
- [ ] 10.5 确认支持的 Windows/Office 版本、Git 平台、文档仓库策略和代码签名流程
- [ ] 10.6 修复试点阻断问题，完成安全扫描、最终回归和 `1.0.0` 发布审批
- [ ] 10.7 发布 `1.0.0` 安装器、哈希和说明，同时保留当前 `.cmd` 作为首期受控回退入口
