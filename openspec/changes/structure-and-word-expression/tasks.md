## 1. 内容层：批量重编号与批量移动计划

- [x] 1.1 在 `doc_tool/application/content/tree.py` 把删除重编号推导推广为通用的移动/插入重编号计划：输入移动集合与目标位置，输出按目标编号排序的无冲突 旧→新 rel_path 列表
- [x] 1.2 在 `tree.py` 实现目录子树移动推导：整棵子树按目标目录编号前缀重写文件与目录名，保持子树内部相对顺序
- [x] 1.3 在 `doc_tool/application/content/refactor.py` 把 `RenamePlan` 扩展为批量计划：一次计划聚合多文件重命名与全部引用编辑，去重与冲突检测升级为跨文件级
- [x] 1.4 在 `refactor.py` 实现批量应用与回滚：复用 `ContentWriter` 备份与改动清单，任一文件失败按逆序回滚到应用前状态
- [x] 1.5 补充内容层自动化测试：同目录排序、跨目录移动、子树移动、目标编号冲突、中途失败回滚、非法目标（自子孙/重名/跨类型）与只读拒绝

## 2. UI 层：章节树拖拽

- [x] 2.1 在 `doc_tool/ui/content/tree_panel.py` 启用拖放：模型声明可拖拽/可放置 flags 并提供 `dropMimeData`，视图实现释放位置判定（目录内追加、文件前插入、非法目标拦截）
- [x] 2.2 拖放释放后调用内容层生成 dry-run 计划，实现预览确认对话框：受影响文件数、引用改动条数、编号冲突与悬空风险标注
- [x] 2.3 确认后调用批量应用并刷新：重建内容索引与树、保持选中与展开状态、当前打开文件重新定位
- [x] 2.4 只读项目禁用拖放入口；拖放被拒绝时显示可理解的拒绝原因
- [x] 2.5 补充 GUI 自动化测试：目标判定、预览内容、非法目标拦截、只读禁用与应用后状态保持

## 3. 构建层：行内格式与真实多级列表

- [x] 3.1 在 `scripts/docx_common.py`/`build_docx.py` 解析阶段识别行内标记（`**`、`*`、`` ` ``），按标记拆分 run 生成 `w:b`/`w:i`/等宽 rStyle
- [x] 3.2 实现真实多级列表：解析 `-`/`*` 与 `N.`/`N、` 列表及缩进层级，生成 `numPr`/`abstractNum` 并维护 numId 实例，保持首项起始编号
- [x] 3.3 补充构建与校验自动化测试：行内格式组合、列表中断/恢复、非列表文本不误转、重复构建一致

## 4. 构建层：超链接、书签与脚注

- [x] 4.1 实现标题书签（`w:bookmarkStart`/`w:bookmarkEnd`）与站内/外部超链接（`w:hyperlink` + 关系表），锚点链接定位到目标书签，书签名稳定且重名去重
- [x] 4.2 实现脚注语法解析与 `w:footnote` 生成（`footnotes.xml` + 正文引用标记），未定义引用键给出可定位警告
- [x] 4.3 扩展 `scripts/validate_docx.py`：识别超链接、书签、列表、脚注事件与元素，纳入事件流对比与模板保留校验，缺失目标给出可定位条目
- [x] 4.4 补充构建与校验自动化测试：外部/站内/锚点链接、缺失目标、书签去重、多脚注编号、非法嵌套

## 5. 集成与人工验收

- [ ] 5.1 运行全量自动化测试与文档构建回归，确认既有文档构建与校验无回归
- [ ] 5.2 人工验证章节树拖拽重排：同目录排序、跨目录移动、子树批量移动、取消、冲突拒绝、只读禁用
- [ ] 5.3 用 Word 打开构建产物，人工验证超链接跳转、书签定位、多级列表、脚注与行内格式显示
- [x] 5.4 运行 PyInstaller 构建与冻结程序冒烟测试，确认拖拽与 Word 表达能力在冻结态正常
