## 1. 内容索引基础设施

- [x] 1.1 定义内容索引数据模型：`FileEntry`（路径、文档类型、行数）、`LineHit`（文件、行号、文本）、`FileReference`（引用类型、目标、位置）、`ContentIndex` 聚合结构，置于 `doc_tool/domain/content_index.py`
- [x] 1.2 实现 `doc_tool/application/content/index.py`：单次扫描 contentRoot 下全部 .md，构建行索引、引用索引、标题清单三份结构
- [x] 1.3 实现索引增量失效：编辑器保存/全局替换/重命名后，标记受影响文件索引失效并支持局部重建
- [x] 1.4 编写索引构建/失效的纯服务单测（不依赖 GUI）

## 2. 引用解析

- [x] 2.1 实现 markdown 链接解析：识别 `[文本](目标)` 与相对路径目标，映射到 contentRoot 文件
- [x] 2.2 实现章节号引用解析：`X.Y` / `X.Y.Z` 与 `content/.../X.Y*.md` 前缀匹配，保守判定不误报
- [x] 2.3 实现文件内标题锚点解析：`##`/`###` 标题生成锚点 id 并支持锚点链接匹配
- [x] 2.4 实现悬空引用检测：引用目标在索引中不存在时标记，区分"确定缺失"与"疑似"
- [x] 2.5 编写引用解析与悬空检测单测（含模糊形态不误报用例）

## 3. 写入安全与回滚

- [x] 3.1 实现 `content_writer.py`：写前 `.md.bak` 覆盖备份、tmp+replace 原子写、跨卷回退 `shutil.move`
- [x] 3.2 实现会话改动清单：JSON 落盘到项目 `.state/`，记录文件、原文本、新文本、替换位置
- [x] 3.3 实现一键回滚：按改动清单反向恢复受影响文件，并触发索引刷新
- [x] 3.4 编写写入安全与回滚单测（含写入失败保留备份用例）

## 4. 章节树导航面板

- [x] 4.1 实现 `doc_tool/ui/content/tree_panel.py`：按 `第X章 → X.Y → X.Y.Z` 渲染目录树
- [x] 4.2 实现节点点击打开文件、树与内容面板联动（结果定位时同步选中并展开父级）
- [x] 4.3 实现只读项目查看态：不可写时禁用编辑器保存/替换/重命名等写操作
- [x] 4.4 编写章节树状态矩阵测试（可写/只读、定位联动）

## 5. 全文搜索

- [x] 5.1 实现 `doc_tool/application/content/search.py`：基于索引的行扫描，支持正则/大小写/整词开关
- [x] 5.2 实现搜索结果集：文件+行+上下文预览，按文档类型过滤，默认 500 条阈值 + "显示更多"
- [x] 5.3 实现 `doc_tool/ui/content/search_panel.py`：搜索输入去抖、结果表展示、点击定位到命中行并高亮
- [x] 5.4 接入 TaskRunner：后台执行搜索、可取消、进度反馈
- [x] 5.5 编写搜索服务单测与结果定位测试

## 6. 内置编辑器与预览

- [x] 6.1 实现 `doc_tool/ui/content/editor_panel.py`：UTF-8 纯文本编辑器，加载/保存
- [x] 6.2 接入 `content_writer` 保存：保存前 `.md.bak`、原子写、保存失败提示与保留备份
- [x] 6.3 实现"在外部编辑器打开"兜底与外部修改检测（文件变化提示刷新索引）
- [x] 6.4 实现侧边 md 预览：复用 build/合并链路的结构解析（标题分级/段落/表格简化/图片占位），编辑去抖刷新，标注近似预览提示
- [x] 6.5 编写编辑器保存/备份/外部修改单测

## 7. 全局替换

- [x] 7.1 实现 `doc_tool/application/content/replace.py`：基于搜索索引执行逐项/全部替换，产出 diff 预览
- [x] 7.2 实现 `doc_tool/ui/content/replace_panel.py`：逐项确认（前后 diff、跳过）与"全部替换"显式汇总确认流
- [x] 7.3 替换写回接入 `content_writer`：每文件 `.md.bak`、改动清单、一键回滚
- [x] 7.4 替换完成后自动触发现有校验管线，结果进结果面板，标记悬空引用
- [x] 7.5 编写替换服务单测（逐项/跳过/全部/回滚/校验触发）

## 8. 章节重命名/重编号联动

- [x] 8.1 实现 `doc_tool/application/content/refactor.py`：基于引用索引扫描受影响引用，生成 dry-run 清单
- [x] 8.2 实现 `doc_tool/ui/content/refactor_panel.py`：dry-run 预览（文件内位置 × 旧→新）、逐条/整体确认后批量更新引用文本与重命名文件
- [x] 8.3 重命名写回接入 `content_writer` 与改动清单回滚；完成后自动跑校验检悬空引用
- [x] 8.4 编写重命名联动单测（dry-run、确认更新、回滚、校验）

## 9. 术语/一致性检查

- [x] 9.1 实现 `content_lint.py` 三类检查器：重复标题、术语大小写不一致、TODO/TBD 残留
- [x] 9.2 实现术语清单 `.state/terms.json` 读写与结果面板增删、变更后重跑相关检查
- [x] 9.3 检查结果复用搜索结果面板展示并支持点击定位
- [x] 9.4 编写术语检查单测

## 10. 主窗口集成与菜单

- [x] 10.1 在 `main_window.py` 增加"内容"工作区布局（章节树 + 搜索/编辑/结果承载界面），与现有摘要/操作/进度区域共存
- [x] 10.2 增加"内容"菜单与快捷键（全文搜索、引用分析、术语检查、全局替换、在外部编辑器打开）
- [x] 10.3 项目打开后自动构建内容索引；只读项目以查看态启用搜索/树，禁用写操作
- [x] 10.4 扩展 `scripts/tests/test_gui_services.py` 覆盖内容操作状态矩阵与关键人工操作路径
- [x] 10.5 更新桌面应用人工操作检查清单（搜索/引用/替换/重命名联动/术语检查路径）

## 11. 打包与收尾

- [x] 11.1 重新运行 PyInstaller/Inno Setup 构建流水线，确认无新运行时依赖与打包回归
- [x] 11.2 全量运行测试（GUI 服务 + 内容操作服务 + 现有管线测试），确认无回归
- [x] 11.3 检查兼容性：保留现有菜单/快捷键/CLI/项目结构，无 schema 变更
