# Security Policy

## Supported Versions

本应用解析并重建 Word (OOXML/DOCX) 文档。由于输入文件可能来自不受信任的
来源，安全修复优先支持当前稳定版本与上一个主要版本。

| 版本 | 支持状态 |
| ---- | -------- |
| 最新稳定版 | ✅ 支持 |
| 上一主要版本 | ✅ 支持（安全修复） |
| 更早版本 | ❌ 不提供安全修复 |

## Reporting a Vulnerability

**请勿通过公开 Issue 提交安全漏洞详情或恶意文档样本。**

请把漏洞报告私密发送到维护团队：

- 私密报告渠道：`[SECURITY_EMAIL]`（正式发布前由权利人填写）
- 请包含：影响版本、复现步骤、最小复现材料（脱敏后）、预期与观察到的行为。

**响应流程：**

1. 收到报告后 72 小时内确认接收。
2. 评估影响并确定受影响的版本范围。
3. 在修复可用后发布安全公告与补丁版本。
4. 在修复发布前，我们会尽力在合理期限内向报告者同步进展（常见 90 天内）。

## Malicious Document Samples

本工具解析任意 DOCX。**不要在公开 Issue 或仓库中直接上传恶意/可疑文档样本**，
这会把样本分发给其他用户。请通过私密渠道提交，并优先提供：

- 触发问题的最小 DOCX 结构片段（XML）或合成复现样本；
- 若只能提供原始文档，请脱敏后再传输。

## Security-relevant Design

- DOCX 解析统一走 `doc_tool/domain/ooxml.py` 的安全入口：拒绝 DTD/实体、
  限制包大小、校验 CRC 与 XML 良构（fail-closed）。
- 项目资源路径经 `ProjectPaths` 做包含校验，拒绝越界引用。
- 公共发行前执行品牌/内网/凭据/OOXML 内容扫描（`packaging/scan_leaks.py`）。
