# 运行依赖（锁定版本）
# 安装：python -m pip install -r requirements.txt
#
# 这些依赖会被 PyInstaller 打包进 onedir 应用，最终用户无需安装 Python 或这些包。

# YAML 解析：读取/写入 project.yml
pyyaml==6.0.3

# XML 解析：OOXML 构建、校验、导入预检
lxml==6.1.1

# 图片处理：截图尺寸计算、DPI 归一化
pillow==12.3.0

# Windows COM 支持：Microsoft Word 自动化刷新 TOC/NUMPAGES
# 仅 Windows 平台需要；PyInstaller 会自动收集 pywin32 DLL
pywin32==312

# 桌面 UI（PySide6 / Qt Widgets）。迁移后不再依赖 tkinter 运行时。
PySide6==6.8.3

# Excel/XLSX 读写：XLSX ↔ CSV/HTML/PDF 互转（table_convert 静态导入，必须声明）
openpyxl==3.1.5

# Review PDF export and verification.
reportlab==4.4.3
pypdf==6.0.0

# PDF 加密 AES-256 支持（未安装时自动降级 RC4-128）
cryptography==50.0.1
