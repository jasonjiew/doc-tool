# 核心效果验收记录

验收口径：只确认“文档拆分”和“维护后重新合并”两个核心效果。

## 1. 文档拆分

- [x] 需求说明书已拆为 `content/requirement/第N章/章节目录/小章节.md`。
- [x] 详细设计说明书已拆为 `content/design/第N章/章节目录/小章节.md`。
- [x] 示例路径符合预期：`第3章 功能需求/3.1 KSHC/3.1.1 登录页.md`、`3.1.2 首页.md`。
- [x] 章节自身存在正文时使用同目录 `_index.md`，合并时位于子章节之前。
- [x] 当前需求文档共 108 个 Markdown，详细设计共 216 个 Markdown。

## 2. 维护后重新合并

- [x] 修改现有 `.md` 后，重新执行可生成包含修改内容的 Word。
- [x] 按连续编号新增 `.md` 后，重新执行可加入正文和目录。
- [x] 删除 `.md` 后，重新执行可同步删除正文和目录项。
- [x] 编号跳号、图片缺失等错误会终止生成，并保留上一次成功输出。
- [x] 项目根目录提供 `生成需求说明书.cmd`、`生成详细设计说明书.cmd`、`全部生成.cmd` 三个双击入口。
- [x] 生成后由 Microsoft Word 刷新目录、页码和总页数字段，并执行严格校验。
- [x] 公司模板的封面、修订记录、样式、编号、分节、页眉、页脚和页面设置均通过校验。

## 抽测结果

| 场景 | 结果 |
|---|---|
| 新增 `3.1.14 注册.md`，包含正文、四级标题、表格和图片 | 通过 |
| 只修改目标 Markdown 段落 | 通过 |
| 删除新增 Markdown | 通过 |
| 跳号新增 `3.1.99` | 正确拒绝并提示应为 `3.1.14` |
| 引用不存在的图片 | 正确拒绝且不覆盖旧 Word |
| 父章节 `_index.md` 顺序 | 通过 |
| Markdown `<br>` 转 Word 真实换行 | 通过 |

## 最终产物

| 文档 | 页数 | 严格校验 |
|---|---:|---:|
| 软件需求说明书 | 349 | 24 PASS / 0 FAIL |
| 系统详细设计说明书 | 576 | 24 PASS / 0 FAIL |

结论：两个核心效果均已实现，可以投入日常章节维护与一键合并使用。

## 3. 任务 7.6 实机回归与视觉抽检清单（Word 正式发布闭环）

前置条件：
- 安装 Microsoft Word 的 Windows 交互式终端
- 已通过「新建项目向导」导入 349 页需求 DOCX 和 576 页详细设计 DOCX
- 两个项目均可通过「打开项目」加载

对每份文档（349 页需求 / 576 页详细设计）独立执行下列步骤，全部通过后方可勾选。

### 3.1 正式合并端到端

- [ ] 「操作 → 正式合并」启动，事件日志依次出现 build / validate_pre / word_refresh / validate_post / publish
- [ ] 「关于/环境诊断」显示 Word 可用（版本 16.0 或更高）
- [ ] 构建阶段显示「构建到临时文件」，不直接写正式输出
- [ ] 前校验通过
- [ ] Word 刷新阶段成功（专用进程，不影响用户已打开的 Word）
- [ ] 后校验通过
- [ ] 原子发布阶段成功，状态栏显示「正式合并成功（Word 已刷新，字段已校验）」
- [ ] output 目录中生成 `<文档编号> <名称>(<版本>).docx` 与同名 `.state.json`
- [ ] 状态文件 `formal=true`、`diagnostic=false`、`failureCode=""`、`outputSha256` 为 64 位指纹

### 3.2 视觉抽检（在 Word 中打开生成的 DOCX）

- [ ] 目录页码连续且与正文标题一致
- [ ] 页眉页脚的 NUMPAGES 字段显示正确总页数（349 / 576）
- [ ] 章节编号连续无跳号
- [ ] 图片完整显示，无红叉
- [ ] 复杂表格样式保留（含嵌套表格）
- [ ] 封面文档编号、版本号正确
- [ ] 修订记录页存在且格式正确

### 3.3 Word 缺失场景

- [ ] 在未安装 Word 的环境（或 CI）执行正式合并
- [ ] 操作前弹出「Microsoft Word 不可用」对话框，提示使用诊断构建
- [ ] 返回错误码 E3001，未获取项目锁
- [ ] 正式输出未被修改（SHA-256 与上次一致）

### 3.4 诊断构建

- [ ] 「操作 → 诊断构建（无 Word）」执行成功
- [ ] 状态栏显示「诊断构建完成（非正式，字段未实机刷新）」
- [ ] 状态文件 `formal=false`、`diagnostic=true`
- [ ] 界面不显示为「正式成功」

### 3.5 用户 Word 隔离

- [ ] 打开用户自己的 Word 文档（任意）
- [ ] 执行正式合并
- [ ] 合并完成后，用户 Word 文档仍保持打开，内容不变
- [ ] 任务管理器中无残留 WINWORD.EXE 进程（专用进程已退出）

### 3.6 失败恢复

- [ ] 故意修改一处 Markdown 引入编号错误（如 3.1.13 → 3.1.99）
- [ ] 执行正式合并，前校验失败，返回 E2002
- [ ] 正式输出仍为上次有效版本（SHA-256 不变）
- [ ] 状态文件 `formal=false`、`failureCode=E2002`
- [ ] 修正编号后重新合并成功，状态恢复 `formal=true`

### 3.7 大文档性能（576 页详细设计）

- [ ] 界面保持响应，事件日志持续更新阶段
- [ ] 进度条显示心跳
- [ ] Word 刷新在配置超时（默认 900 秒）内完成
- [ ] 全程无「无响应」或 COM 超时

### 3.8 回归结论

- [ ] 349 页需求文档：上述 3.1-3.7 全部通过
- [ ] 576 页详细设计文档：上述 3.1-3.7 全部通过

参考：自动测试见 `scripts/tests/test_word_release.py`，覆盖 Word 缺失/超时/保存失败/后校验失败保留旧输出的破坏性场景（26 项全部通过）。
